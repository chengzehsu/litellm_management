# LiteLLM Proxy Package (includes all proxy dependencies like backoff)
litellm[proxy]>=1.40.0

# Prisma for database ORM (required by LiteLLM proxy)
prisma>=0.11.0

# Database drivers
psycopg2-binary>=2.9.0

# Additional dependencies for Langfuse integration
httpx>=0.24.0
pydantic>=2.0.0

